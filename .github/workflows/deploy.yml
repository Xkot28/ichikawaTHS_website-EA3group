import os

# 設定ファイルを保存するディレクトリを作成
workflow_dir = 'github_actions_setup/.github/workflows'
os.makedirs(workflow_dir, exist_ok=True)

# deploy.yml の内容
deploy_yml_content = """name: Build and Deploy

on:
  push:
    branches:
      - main  # メインブランチにプッシュされた時に実行

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run Update Script
        run: |
          # ここでColabで行っていた更新処理を自動実行するように設定できます
          # 現時点では、Colabで生成したファイルをそのままアップロードする運用が簡単です
          echo "Updating site..."

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: . # リポジトリのルートをデプロイ
          branch: gh-pages # 公開用のブランチ（Pagesの設定でこれを選択します）
"""

# ファイル書き出し
with open(os.path.join(workflow_dir, 'deploy.yml'), 'w', encoding='utf-8') as f:
    f.write(deploy_yml_content)

print(f"✅ 設定ファイルを作成しました: {workflow_dir}/deploy.yml")
print("このファイルをGitHubリポジトリの同じ構造（.github/workflows/）にアップロードしてください。")
